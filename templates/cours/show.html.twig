{% extends 'base.html.twig' %}

{% block title %}Cours{% endblock %}

{% block body %}
    <div style="float: right;margin-right:20px">
        <a href="{{ path('app_cours_index') }}"><i class="fa fa-fw fa-rotate-left" style="font-size: 40px;color: #0f0f0f" aria-hidden="true"></i></a>
        <a href="{{ path('app_cours_edit', {'id': cour.id}) }}" style="margin-right:20px"><i class="fa fa-fw fa-edit" style="font-size: 40px;color: #0f0f0f" aria-hidden="true"></i></a>
    </div>
    <h1>Détail du cours : {{ cour.nom }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ cour.nom }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('cours/_delete_form.html.twig') }}
{% endblock %}
