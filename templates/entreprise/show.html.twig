{% extends 'base.html.twig' %}

{% block title %}Entreprise{% endblock %}

{% block body %}
    <h1>Entreprise</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entreprise.id }}</td>
            </tr>
            <tr>
                <th>EleveId</th>
                <td>{{ entreprise.eleveId }}</td>
            </tr>
            <tr>
                <th>Semaine</th>
                <td>{{ entreprise.semaine }}</td>
            </tr>
            <tr>
                <th>ActiPrev</th>
                <td>{{ entreprise.actiPrev }}</td>
            </tr>
            <tr>
                <th>ActiRea</th>
                <td>{{ entreprise.actiRea }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ entreprise.commentaire }}</td>
            </tr>
            <tr>
                <th>CompeMiseEnEouvre</th>
                <td>{{ entreprise.compeMiseEnEouvre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_entreprise_index') }}">back to list</a>

    <a href="{{ path('app_entreprise_edit', {'id': entreprise.id}) }}">edit</a>

    {{ include('entreprise/_delete_form.html.twig') }}
{% endblock %}
