{% extends 'base.html.twig' %}

{% block title %}Eleve{% endblock %}

{% block body %}
    <div style="float: right;margin-right:20px">
        <a href="{{ path('app_eleve_index') }}"><i class="fa fa-fw fa-rotate-left" style="font-size: 40px;color: #0f0f0f" aria-hidden="true"></i></a>
        <a href="{{ path('app_eleve_edit', {'id': eleve.id}) }}" style="margin-right:20px"><i class="fa fa-fw fa-edit" style="font-size: 40px;color: #0f0f0f" aria-hidden="true"></i></a>
    </div>
    <h1>Détail de l'élève : {{ eleve.nom }} {{ eleve.prenom }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ eleve.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ eleve.prenom }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('eleve/_delete_form.html.twig') }}
{% endblock %}
