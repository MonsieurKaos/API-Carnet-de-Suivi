<form method="post" action="{{ path('app_ecole1_delete', {'id': ecole1.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ecole1.id) }}">
    <button class="btn">Delete</button>
</form>
